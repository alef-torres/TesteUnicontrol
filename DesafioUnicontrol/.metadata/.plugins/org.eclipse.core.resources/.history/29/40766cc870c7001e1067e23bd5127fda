@import url('https://fonts.googleapis.com/css2?family=Poppins:wght@100;300;400;500;600;700&display=swap');

* {
    margin: 0;
    padding: 0;
    font-family: 'Poppins';
}

body {
    width: 100%;
    height: 100vh;
    background: linear-gradient(45deg, #7DFFE3, #fff);
}

.principal {
    margin-top: auto;
    height: 100%;
    display: flex;
    flex-direction: row;
    justify-content: space-around;
    align-items: center;
}

.formulario-de-cadastro {
    display: flex;
    flex-direction: column;
    padding: 2rem;
    background: #fff;
    border-radius: 10px;
}

.input {
    border-radius: 22px;
    border: 2px #6f7e8a solid;
    padding: 3px;
    box-shadow: 4px 4px 4px rgba(0, 0, 0, 0.25);
}

.input i {
    margin-left: 5px;
}

input {
    border: none;
    outline: none;
}

#btn {
    display: flex;
    flex-direction: row;
    align-items: center;
    justify-content: space-around;
    margin-top: 1rem;
}

button {
    width: 80px;
    height: 30px;
    border: solid 1px black;
    border-radius: 22px;
    background: linear-gradient(45deg, #7DFFE3, #fff);
    color: black;
    cursor: pointer;
    box-shadow: 4px 4px 4px rgba(0, 0, 0, 0.25);
}

#btn-enviar {
    background: linear-gradient(-45deg, #7DFFE3, #fff);
}